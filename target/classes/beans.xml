<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd">
    <!--告诉spring创建对象
        声明bean
        id：对象的自定义名称，唯一值，spring通过这个名称找到对象
        class：类的全限定名称

        spring把创建好的对象放入map中，例如：
            sprintMap.put("someService", new SomeServiceImpl())

        一个bean标签声明一个对象，
    -->
    <bean id="someService" class="com.xlz.spring_test.service.impl.SomeServiceImpl" />
</beans>

<!--
    spring的配置文件
    1.beans ：是根标签，spring把java对象称为bean
    2,。spring-beans.xsd 是约束文件，和 mybatis指定，dtd是一样的
-->